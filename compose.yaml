services:
  api:
    build: ./api/EatAndWatch
    ports:
      - "5144:5144"
    environment:
      - ASPNETCORE_URLS=http://+:5144
    volumes:
      - ./api/EatAndWatch/EatAndWatch/data:/app/data


  web:
    build: ./weboldal
    ports:
      - "8080:8080"
    depends_on:
      - api
